#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    // int num = 25; // regular assignment
    // int *pnum = &num; // * on left == pointer symbol
    // int nopnum = *pnum; // * on right == dereference symbol
    // printf("%d\n", num);
    // printf("%p\n", pnum);
    // printf("%d\n", nopnum);
    // char *str = "Brian";
    // char c = 'a';
    // char *restOfString = strchr(str, c);
    // printf("%s\n", restOfString);
    // printf("%d\n", c);
    // char str[] = "Brian";
    // char subStr[] = "ia";
    // char *match = strstr(str, subStr);
    // printf("%s\n", match);
    // char res[sizeof(subStr)];
    // for(int i = 0; i < strlen(subStr); i++)
    // {
    //     res[i] = match[i];
    // }
    // printf("%s\n", res);
    // printf("%d\n", islower('w'));
    // char letter = 'b';
    // char upperLetter = toupper(letter);
    // printf("%c\n", upperLetter);
    // char l = 'a';
    // int charCodeAt = l;
    // printf("%d\n", charCodeAt);
    return 0;
}