#include <stdio.h>

int main()
{
    // switch statement
    // one of several alternatives needed
    /*
        switch(expression)
        {
            case v1: if v1 == expression then evaluate
                staement
                break
            case v2: if v2 == expression then evaluate
                statement
                break
            default: if v3 == expression then evaluate
                statement
                break
        } 
    */
    // simple constants or constant expressions
    // break case executes switch statement
    // leave break out if you want to execute code
    // with different blocks of code.
    // default is the fall back value (like else)
    // enum Weekday {Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};
    // enum Weekday today = Friday;
    // switch(100)
    // {
    //     case Monday:
    //         printf("Monday\n");
    //         break;
    //     case Friday:
    //         printf("Friday\n");
    //         break;
    //     case Saturday:
    //         printf("Saturday\n");
    //         break;
    //     case Sunday:
    //         printf("Sunday\n");
    //         break;
    //     default:
    //         printf("Day does not exist.");
    //         break;
    // }

    return 0;
}